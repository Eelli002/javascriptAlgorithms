 function insertionSort(arr) {
    for (let i = 0; i < arr.length; i++) {
        var currentVal = arr[i];
        for (let j = i - 1; j >= 0 && arr[j] > currentVal; j--) {
            arr[j+1] = arr[j]
        }
        arr[j+1] = currentVal;
    }
    return arr;
 }

We do not need our swap function bc we save the variable into currentVal and then assign it to its proper position later.